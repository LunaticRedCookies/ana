import React from 'react'
import ReactDOM from 'react-dom/client'

function App() {
  return <div>Edge Discovery Bot Dashboard (placeholder)</div>
}

ReactDOM.createRoot(document.getElementById('root')!).render(<App />)
